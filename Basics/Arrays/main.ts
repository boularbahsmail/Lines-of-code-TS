
// Arrays
let nums: number[]; // let words: string[];
nums = [1, 2, 3];

// Use JavaScript array methods
nums.push(4);
nums.unshift(0);

console.log(nums);

// Doublicating all array elements
const doubleIt = nums.map(e => e* 2);
console.log(doubleIt);
